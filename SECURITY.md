# Security policy

Do not open public issues containing vulnerabilities, credentials, private
repository content, or exploit details. Use GitHub private vulnerability
reporting once enabled. Until then, contact the repository owner privately.

Flow42 never needs users to paste Forge tokens into configuration. It relies on
existing authenticated `gh` or `glab` sessions and never prints secret values.
