# Plan: {{title}}

## Vertical slices

For each slice record outcome, dependencies, owned files, proving tests,
branch/worktree, integration order, and rollback.

## Parallelization map

Record the task schedule graph: job ID, dependencies, synchronization barrier,
model profile, worktree, owned paths, retry limit, and `delegation_allowed: false`.

## Data contracts

Record the data flow graph separately. For every edge name the artifact, schema
version, producer, consumers, provenance fields, validator, and failure behavior.

## Integration order

## Risks and rollback
