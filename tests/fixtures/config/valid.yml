schema_version: 1
forge: auto
harness: auto
execution_environment: auto
base_branch: auto
concurrency: 4
worktree_parent: auto
model_profiles:
  frontier: auto
  worker: auto
  utility: auto
commands:
  format: [auto]
  lint: [shellcheck, tests/contracts.sh]
  typecheck: []
  test: [sh, tests/contracts.sh]
  build: []
protected_paths: []
mandatory_gates:
  - high-risk-plan
  - irreversible-action
  - merge
  - deploy
